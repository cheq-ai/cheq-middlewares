module.exports = {
	errInvalidParams: 'invalid params',
	errThreatTypesCodes: 'blockRedirect and captcha must be array',
	errSameThreatTypesCodes: 'threat type in blockRedirect and captcha must be uniq for each array',
	errInvalidURIExclusionArray: 'invalid URIExclusion should be an array regular expressions or strings',
	errInvalidApiEndpoint: 'api endpoint is missing or invalid',
	errInvalidTagHash: 'tagHash is missing or invalid',
	errInvalidApiKey: 'apiKey is missing or invalid',
	errApiRequest : 'error while sending api request'
};